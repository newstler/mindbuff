<nav class="navbar navbar-default <%= yield :navbar_classes %>" role="navigation">
	<div class="container">
		<div class="row">
			<div class="col-md-12">
				  <!-- Brand and toggle get grouped for better mobile display -->
				  <div class="navbar-header">
				    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
				      <span class="sr-only">Toggle navigation</span>
				      <span class="icon-bar"></span>
				      <span class="icon-bar"></span>
				      <span class="icon-bar"></span>
				    </button>
				    <%= link_to "MindBuff!", :nodes, class: "navbar-brand" %>
				  </div>

				  <!-- Collect the nav links, forms, and other content for toggling -->
				  <div class="collapse navbar-collapse" id="navbar-collapse">
				    <ul class="nav navbar-nav">
					    <li>
						    <%= link_to :new_node do %>
						      	<i class="fa fa-plus"></i>
						    <% end %>
					    </li>
					</ul>

					<%= form_tag nodes_path, method: :get, class: "form-inline navbar-left col-md-3 search_form", role: "search" do %>
						<div class="input-group">
							<%= text_field_tag :tags, params[:tags], placeholder: "Search", class: "form-control" %>
							<div class="input-group-btn">
								<%= button_tag class: "btn btn-default", type: :submit do %>
									<i class="fa fa-search"></i>
								<% end %>
							</div>
						</div>
					<% end %> 

					<ul class="nav navbar-nav navbar-right">
						<li>
							<%= link_to "Sign in with Facebook", "#" unless current_user %>
							<%= link_to "Sign out", destroy_user_session_path, method: :delete if current_user %>
						</li>
					</ul>

				  </div><!-- /.navbar-collapse -->
  			</div>
		</div>
	</div>
</nav>